"""
Pydantic models — auto-generated from `shared/schemas/*.json` by `codegen.sh`.

Currently empty — codegen has not run yet (deferred to after Step 3 — see
`JOURNAL.md` Active TODOs and `shared/schemas/README.md`). Routes and
agents temporarily use inline `_PerceptionStub` etc. until this folder
is populated.

DO NOT WRITE FILES HERE BY HAND — they will be overwritten on next codegen run.
"""
