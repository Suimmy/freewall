# Optional Procfile fallback — copy to /backend/Procfile if Railway needs explicit start command.
# Most cases railway.toml's startCommand suffices.
web: uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT
